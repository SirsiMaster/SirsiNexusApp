{
  "name": "sirsi-nexus",
  "private": true,
  "workspaces": [
    "ui",
    "cli"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint",
    "dev": "netlify dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cookie": "^1.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^23.3.0"
  }
}
